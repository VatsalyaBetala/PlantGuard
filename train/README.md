# Training

Training scripts and architectures live here to keep UI/app inference code decoupled.

- `train.py`: example CNN training loop
- `model.py`: simple CNN architecture used by the training script

These scripts can import shared preprocessing utilities from `src/` as needed.
